## Color picker

### Descripción

Esta es una extension de Google chrome que permite tomar el color de cualquier parte de tu pantalla

### Instalación

````
npm i
npm run build
````

Luego en Chrome se abre el administrador de extenciones, se habilita el modo desarrollador, y finalmente se agrega como archivo comprimido la carpeta `dist` que genero el `npm run build`